<% @page_title = "Admin Login"%>

<div class="login">
	<%= form_tag(:action => 'attempt_login') do%>
    	<p>User Name: <%= text_field_tag('user_name', params[:user_name])%> </p/>
    	<p>Password: <%= password_field_tag('password')%></p/>
    <%= submit_tag('Log In')%>
	<% end %>
</div>	
