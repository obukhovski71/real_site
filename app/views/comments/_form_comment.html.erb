<div class = "commentpartial">
	<%= form_for(@comment, :url => {:action => 'create', :photo_id => @photo.id}) do |f|%>
	<table summary = "comment form">
		<tr>
			<td><%= f.text_field(:body) %></td>
			<td><%= submit_tag("Submit Comment")%></td>
		</tr>
	</table>
	<%end%>
</div>